# Leave unset in development: Vite proxies /api to the server (see vite.config.ts).
# Set this when the client and API are deployed to different domains, e.g.
# VITE_API_URL=https://roadmap-api.example.com/api/v1
VITE_API_URL=
